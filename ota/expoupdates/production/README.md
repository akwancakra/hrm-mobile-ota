# Expo Updates (Self-Hosted) — Production

Directory ini untuk eksperimen/migrasi ke `expo-updates` format di masa depan.

Saat ini belum digunakan. Custom OTA masih aktif di `ota/android/production/` dan `ota/ios/production/`.
